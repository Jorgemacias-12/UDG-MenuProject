name: project CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Say hello world
        run: echo Hola mundo!
      - name: build app
        run: make
